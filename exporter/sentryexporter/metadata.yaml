type: sentry

status:
  class: exporter
  stability:
    beta: [traces, logs]
  distributions: [contrib]
  codeowners:
    active: [AbhiPrasad]


tests:
  expect_consumer_error: true
  config:
    dsn_mode:
      dsn: https://key@host/path/42